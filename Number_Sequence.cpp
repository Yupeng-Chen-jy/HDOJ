#include <stdio.h>

void solve(int a,int b,int n) {
    
}

int main(int argc, char const *argv[])
{
    int a,b;
    long long int n;
    while (scanf("%d %d %lld",&a,&b,&n) != EOF && n != 0) {
        solve(a,b,n);
    }

    return 0;
}
