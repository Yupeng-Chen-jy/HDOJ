#include <stdio.h>

void solve(int t) {
    int color[t];
    char s[20];
    for (int i = 0;i < t;i ++) {
        scanf("%s",s);
    }
}

int main(int argc, char const *argv[])
{
    int t;
    while (scanf("%d",&t) != EOF && t != 0) {
        solve(t);
    }
    return 0;
}
